#For extending the pps signal line information provided by the Multisense Head

Header header

time     pps            # Internal PPS signal time
duration base_offset    # Offset to base clock time
bool     synchronized   # Flag if the clocks are synchronized

